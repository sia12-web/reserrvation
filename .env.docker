# Docker environment variables
# These are overridden by docker-compose.yml but kept here as defaults

DATABASE_URL=postgres://postgres:postgres@postgres:5432/reservation
DB_URL=postgresql://postgres:postgres@postgres:5432/reservation
REDIS_URL=redis://redis:6379
PORT=3000
NODE_ENV=production

SMTP_HOST=mailhog
SMTP_PORT=1025
MAIL_FROM=no-reply@reservation.com

# Stripe - Replace with real values for production
STRIPE_SECRET_KEY=sk_test_placeholder
STRIPE_WEBHOOK_SECRET=whsec_placeholder
